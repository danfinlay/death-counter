{
  "name": "death_counter",
  "version": "1.0.0",
  "description": "Counting causes of death from the CDC's dataset.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "csv-stream": "^0.1.3",
    "lodash": "^3.10.1"
  }
}
